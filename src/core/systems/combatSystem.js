// combatSystem — våben-dps, opsluging/invasion (størrelse), skade, fag-infektion og -spredning.
// Spejler kampmodellen i balance/simulate.js. Se ENEMIES.md + BALANCE.md §8.
/** @param {import('../state.js').WorldState} state @param {number} dt */
export function combatSystem(state, dt) {
  // TODO: brug spatial.queryRadius til at finde mål; påfør dps; håndtér opsluging når
  //       radius-overtag; spred fager ved burst.
  return state;
}

// colonySystem — klynge-detektion (nærhed via spatial), ressourcedeling (nanorør), cross-feeding
// af biprodukter, biofilm-forsvar og quorum-tærskler. Se SOCIAL.md §2.
/** @param {import('../state.js').WorldState} state @param {number} dt */
export function colonySystem(state, dt) {
  // TODO: gruppér nærtstående slægtsceller i kolonier; udlign ATP/byggesten; tildel
  //       biofilm-/quorum-bonusser; sæt lineage.colonyId.
  return state;
}

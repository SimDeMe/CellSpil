// spawnSystem — føde, fjender og farezoner efter pacing/ankre (config.Enemies.spawnAfter osv.).
// Se ENEMIES.md §6 + BALANCE.md §1.
/** @param {import('../state.js').WorldState} state @param {number} dt */
export function spawnSystem(state, dt) {
  // TODO: tæl timere; spawn mad op til cap; introducér fjendetyper ved deres spawnAfter;
  //       skab farezoner med interval.
  return state;
}

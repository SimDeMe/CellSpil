// Tegner celler: organisk deformation ved fart, trivsels-glød (tolerance.comfort), flagel/pili.
// Se SPEC.md §11 + ENVIRONMENT.md §2(d). Kun læsning af state.
export function drawCells(state, camera, alpha) {
  // TODO: for hver kind==='cell'/'enemy' — tegn form, kerne, motorik, glød efter comfort.
}

// Tegner miljøet: diskret baggrundsstemning + valgbar linse (ét overlay ad gangen).
// Se ENVIRONMENT.md §2(a,b). Kun læsning.
export function drawFields(state, camera, activeLens = null) {
  // TODO: blød baggrundstone (lys/temp); hvis activeLens — heatmap af den parameter.
}

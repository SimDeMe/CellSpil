// Inspector: mutationstræ (datadrevet fra config-gener) + komfortpanel (tolerance-strimler) +
// slægts-panel (kolonien). Se EVOLUTION.md, ENVIRONMENT.md §3, SOCIAL.md §5.
export function createInspector(root /* HTMLElement */, emitIntent) {
  // TODO: render faner; mutationskøb og rolle-tildeling sendes via emitIntent(intent).
  return {
    update(state) { /* TODO */ },
  };
}

// HUD: ressource-bjælker (ATP/amino/nukleotid), generation, population, lokale miljø-målere.
// Se SPEC.md §10 + ENVIRONMENT.md §2(c). Kun læsning; brugerhandlinger → intents.
export function createHud(root /* HTMLElement */) {
  // TODO: byg DOM-elementer.
  return {
    update(state) {
      // TODO: opdater bjælker fra den styrede celle + fields.sample ved dens position.
    },
  };
}

// Minimap: overblik over verden, fjender, egne celler, farezoner — og samme linse som overlayet.
// Se SPEC.md §10. Kun læsning.
export function createMinimap(canvas /* HTMLCanvasElement */) {
  // TODO: tegn skaleret oversigt fra state hver frame.
  return {
    update(state) { /* TODO */ },
  };
}

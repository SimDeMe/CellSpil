// Kontrolknapper: flok-politik (saml/forager/forsvar/jagt), kald-sammen, linse-vælger, pause.
// Se SOCIAL.md §5. Sender brugervalg som intents.
export function createControls(root /* HTMLElement */, emitIntent) {
  // TODO: byg knaprække; klik → emitIntent({ type:'set-policy', ... }) osv.
  return {
    update(state) { /* TODO */ },
  };
}
